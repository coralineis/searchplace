<div class="container">
  <div class="mt-3 text-success"><%= notice %></div>

  <%= render 'search_form', q: @q %>

  <%= render 'public/place_genres/genre_list', place_genres: @place_genres %>

  <%= render 'search_list', search_places: @search_places %>

  <%= paginate @places %>

</div>